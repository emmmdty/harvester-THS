services:
  rednote-harvest:
    build: .
    ports:
      - "3000:3000"
    environment:
      HOST: 0.0.0.0
      HEADLESS: "1"
      DOCKER: "1"
    volumes:
      - ./output:/app/output
      - ./.xhs-profile:/app/.xhs-profile
      - ./.douyin-profile:/app/.douyin-profile
